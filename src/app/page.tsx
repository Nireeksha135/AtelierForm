// Placeholder entry point. The persistent WebGL canvas, Lenis scroll
// controller, and section overlays are introduced in the next commits.
// This exists only to verify the Next.js + Tailwind pipeline compiles.
export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <h1 className="font-editorial text-4xl tracking-tightest text-studio">
        Atelier Form
      </h1>
    </main>
  );
}
